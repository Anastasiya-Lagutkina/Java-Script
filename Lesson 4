//Ваши фамилия и имя
console.log('Nastya Lagutkina');

//Расскажите коротко о себе:
//Меня зовут Настя. Мне 24 года. Я мечтаю стать программистом.

//Объясните, почему код работает без ошибок, 
//хотя переменная а создана дважды с помощью ключевого слова let
//(уберите комментарий в данном фрагменте и запустите программу).
/*
let a = 1;
console.log(a);
{
  let a = 10;
  console.log(a);
}
*/
//Код работает без ошибки, потому что разные области видимости.


//Объясните, почему код работает с ошибкой 
//(уберите комментарий в данном фрагменте и запустите программу).
/*
{
  let a = 1;
  let a = 2;
  console.log(a);
}
*/
//Код работает с ошибкой, потому что при использовании let в одной области видимости при переопределении переменной слово let использовать не нужно.


//Объясните, почему код работает с ошибкой 
//(уберите комментарий в данном фрагменте и запустите программу).
/*
let a = 1;
const a = 2;
console.log(a);
*/
//Код работает с ошибкой, потому что let и const нельзя объявлять повторно в одном блоке.

//Объясните, почему код работает без ошибок, 
//хотя переменной а присвоено значение без ключевого слова let 
//(уберите комментарий в данном фрагменте и запустите программу).

a = 1;
console.log(a);
{
  var a = 10;
}
console.log(a);
